PyQt5>=5.15
pynput>=1.7          # cross-platform hotkey + mouse simulation (Windows also works without it)

# Optional platform-specific dependencies:
# macOS click-through support:
#   pip install pyobjc-framework-Cocoa
# Linux (X11) click-through support:
#   pip install python-xlib
